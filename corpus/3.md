
Update(CLAUDE.md)<br/>Added 3 lines, removed 3 lines<br/>30 | 链路 | 现实吞吐 | 结论 |<br/>31 |----|----|----|<br/>32 | SCI (XDS110 VCP) | 几 Mbps | bring-up 哑泵专用 |<br/>33 -| CAN-FD | 有效 2-3 Mbps | 出局 |<br/>34 -| W5500/SPI 以太网桥 | 个位数 Mbps | 出局 |<br/>35 -| FSI | 50-100 Mbps 但 PC 不认，需自制桥 | 出局 |<br/>33 +| CAN-FD | 有效 2-3 Mbps | 排除 |<br/>34 +| W5500/SPI 以太网桥 | 个位数 Mbps | 排除 |<br/>35 +| FSI | 50-100 Mbps 但 PC 不认，需自制桥 | 排除 |<br/>36 | **EtherCAT** | 100 Mbps 线速，实用 8-10 MB/s | **终点，唯一幸存者** |<br/>37 |<br/>38 **阶梯只有两级：SCI 哑泵（Phase 3.5，验证契约与架构） → EtherCAT（Phase 6，正赛）。**<br/>协议（block、描述符）活在管道里面，换物理层只换管道。





Update(CLAUDE.md)<br/>Added 1 line, removed 1 line<br/>77 - C2000Ware / MotorControl SDK / DCL 全是 C，平台目标用户（新手）的语言也是 C; ODrive<br/>    的重 C++ 是其学习门槛的一部分，不复制；<br/>78 - 规避全局对象构造顺序、隐式构造等裸机 C++ 坑。<br/>79<br/>80 - 弥补 C++ 缺失的三条纪律（对齐 TI DCL 风格）：多实例 = struct 实例 + 操作函数（`pi_up<br/>-date(&pi_vel, err)`）; 命名空间 = 模块前缀; init 完成后禁止动态分配。<br/>80 + 弥补 C++ 缺失的三条编码约定（对齐 TI DCL 风格）：多实例 = struct 实例 + 操作函数（`p<br/>i_update(&pi_vel, err)`）; 命名空间 = 模块前缀; init 完成后禁止动态分配。<br/>81<br/>82 **PC 侧统一用 Rust + egui**: L2 控制库编译成动态库，经 FFI（bindgen）接入 Rust 做单<br/>元测试与电机模型仿真，egui_plot 看波形。仿真宿主和上位机是同一个程序的三个数据源（`S<br/>imSource` / `MywaySource` / `V2kSource`）。<br/>83
